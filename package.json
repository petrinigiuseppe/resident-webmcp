{
  "name": "resident-webmcp",
  "version": "1.0.0",
  "private": false,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check:syntax": "node --check crate/crate.js && node --check crate/webmcp.js && node --check crate/song-dna.js && node --check crate/agent-state.js && node --check crate/webmcp-debug.js && node --check scripts/check_live_surface.mjs && node --check scripts/check_webmcp_contract.mjs",
    "check:live": "node scripts/check_live_surface.mjs https://demo.sephmartin.com",
    "check:webmcp:contract": "node scripts/check_webmcp_contract.mjs"
  }
}
